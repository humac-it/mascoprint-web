# =============================================================================
# Mascoprint Website - Environment Variables
# =============================================================================
# Copy this file to .env and fill in the values:
#   cp .env.example .env
#
# Variables prefixed with NEXT_PUBLIC_ are exposed to the browser.
# All other variables are server-side only.
# =============================================================================

# -----------------------------------------------------------------------------
# Analytics
# -----------------------------------------------------------------------------
# Google Analytics measurement ID (e.g. G-XXXXXXXXXX)
# Optional — leave blank to disable analytics
NEXT_PUBLIC_GA_ID=

# -----------------------------------------------------------------------------
# Contact Form - Email (Resend)
# -----------------------------------------------------------------------------
# API key from https://resend.com/api-keys (starts with re_)
# Required for the contact form to send emails
RESEND_API_KEY=

# Recipient email address for contact form submissions
CONTACT_EMAIL_TO=office@mascoprint.co.uk

# -----------------------------------------------------------------------------
# Contact Form - Cloudflare Turnstile (spam protection)
# -----------------------------------------------------------------------------
# Create a widget at https://dash.cloudflare.com → Turnstile
# For local development, use Cloudflare's test keys:
#   Site key:   1x00000000000000000000AA (always passes)
#   Secret key: 1x0000000000000000000000000000000AA (always passes)
#
# NEXT_PUBLIC_TURNSTILE_SITE_KEY is baked into the client bundle at build time.
# If you change it, you must trigger a rebuild on Cloudflare Pages.
NEXT_PUBLIC_TURNSTILE_SITE_KEY=
TURNSTILE_SECRET_KEY=

# -----------------------------------------------------------------------------
# Site Configuration
# -----------------------------------------------------------------------------
NEXT_PUBLIC_SITE_URL=https://mascoprint.co.uk
